# Point this to your backend once it's running.
# Leave as-is for now — the app runs entirely on mock data.
VITE_API_BASE_URL=

# Set to "false" once real endpoints are wired up in your hooks.
VITE_USE_MOCK_API=true
